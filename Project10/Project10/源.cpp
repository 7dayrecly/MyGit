#include<iostream>

using namespace std;

int main()
{
	/*
	char s[] = "abcdefg", *ps;
	ps = s;
	*(ps + 4) += 3;
	cout << *ps << endl;
	cout << *(ps + 4);
	*/
	/*
	char s[] = "12345";
	char *ps1, *ps2;
	ps1 = s;
	ps2 = ps1 + 2;
	if (ps1 < ps2)
	{
		cout << "ps1 and ps2 are :" << ps1 << "  and  " << ps2 << endl;
		cout << ps1 << endl;
	}
	else {

		cout << "ps1 and ps2 are the same " << ps1 << endl;
	}*/

	/*
	int i = 1, sum = 0;
	while (i <= 15)
		if (i++ % 3 != 2)
			continue;
		else
			sum += i;
	cout << "sum = " << sum << endl;
	*/


}